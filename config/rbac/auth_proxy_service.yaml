apiVersion: v1
kind: Service
metadata:
  labels:
    odf-fs: odf-fs-workspace
  name: ibm-storage-odf-operator-metrics-service
  namespace: openshift-storage
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    odf-fs: odf-fs-workspace
